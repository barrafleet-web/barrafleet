site_url: https://barratapas.pages.dev

backend:
  name: github
  repo: barrafleet-web/barratapas
  branch: main
  base_url: https://github-oauth.barrafleet.workers.dev
  auth_endpoint: auth

# REQUIRED (this is what your error is about)
media_library:
  name: uploadcare
  config:
    publicKey: "PASTE_YOUR_UPLOADCARE_PUBLIC_KEY_HERE"

media_folder: "uploads"
public_folder: "/uploads"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - file: "index.html"
        label: "Homepage"
        name: "home"
        fields:
          - { label: "Body", name: "body", widget: "text" }
